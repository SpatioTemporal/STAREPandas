"""Placeholder for cloud client config (filled in at C-6).

Will read ``endpoint`` and ``api_key`` from the same ``.config`` file that
``staredataframe._load_config_from_default_locations`` consumes, so cloud
callers reuse the existing config path.
"""
